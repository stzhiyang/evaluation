# 系统性能消耗评估功能

## 概述

系统性能消耗评估功能用于测量单个算法对系统资源（CPU 和内存）的实际消耗。通过对比算法启动前后的系统资源占用，精确计算出算法本身的资源开销，为算法优化和系统部署提供量化依据。

## 功能特点

### 1. 基线对比测量
- **启动前基线**：测量算法启动前的系统资源占用
- **运行时测量**：测量算法稳定运行时的系统资源占用
- **增量计算**：自动计算算法实际消耗 = 运行时 - 基线

### 2. 多维度资源监控
- **CPU 使用率**：百分比形式，精确到小数点后两位
- **内存使用量**：以 MB 为单位，包含物理内存和虚拟内存
- **统计分析**：提供平均值和标准差，评估稳定性

### 3. 自动化评估流程
- **节点检测**：自动检测算法节点是否启动
- **分阶段测量**：自动完成基线和运行时两阶段测量
- **智能等待**：提示用户在合适时机启动算法

### 4. 详细报告生成
- **文本报告**：生成易读的 TXT 格式报告
- **JSON 数据**：保存原始数据供后续分析
- **时间戳标识**：每次评估生成独立的结果目录

## 评估原理

### 测量方法

```
系统总资源占用 = 基础系统 + 其他程序 + 目标算法

基线测量（算法未启动）:
  基线资源 = 基础系统 + 其他程序

运行时测量（算法已启动）:
  运行时资源 = 基础系统 + 其他程序 + 目标算法

算法实际消耗:
  算法消耗 = 运行时资源 - 基线资源
```

### 为什么需要基线对比？

1. **排除系统干扰**：系统本身和其他程序会占用资源
2. **精确测量**：只计算目标算法的实际消耗
3. **公平对比**：不同时间、不同环境下的测量可以对比

## 文件结构

```
evaluation/
├── launch/
│   └── performance_evaluation.launch    # 启动文件
├── config/
│   └── performance_config.yaml          # 配置文件
├── scripts/
│   └── performance_evaluator.py         # 评估脚本
└── results/
    └── performance_<算法名>_<时间戳>/   # 结果目录
        ├── performance_report_<时间戳>.txt   # 文本报告
        └── performance_data_<时间戳>.json    # 原始数据
```

## 使用方法

### 步骤 1：配置评估参数

编辑 `evaluation/config/performance_config.yaml`：

```yaml
# 算法名称（用于显示和文件命名）
# 可选: "M-detector", "LVDOT", "LDOT", "FAPP"
algorithm_name: "M-detector"

# ROS 节点名称（用于检查节点是否运行）
# 对应关系:
#   M-detector -> dynfilter
#   LV-DOT -> detector_node
#   LDOT -> ldot_detector_node
#   FAPP -> mapping_node
node_name: "dynfilter"

# 监控频率 (Hz)
# 建议: 1-5 Hz，太高会影响系统性能
monitor_freq: 1.0

# 基线测量时长（秒）
# 在算法启动前测量系统基线资源占用
# 建议: 10-30 秒，时间越长基线越准确
baseline_duration: 30.0

# 运行测量时长（秒）
# 算法启动后测量系统资源占用
# 建议: 60-120 秒，时间越长统计越稳定
running_duration: 60.0

# 输出目录
# 相对于 evaluation 包的路径
output_dir: "results/"
```

### 步骤 2：启动评估

**重要：确保算法节点未启动！**

在终端执行：

```bash
roslaunch evaluation performance_evaluation.launch
```

### 步骤 3：等待基线测量

评估器会自动进行基线测量：

```
==================================================
单算法性能评估器已初始化
==================================================
算法名称: M-detector
节点名称: dynfilter
监控频率: 1.0 Hz
基线测量时长: 30.0 秒
运行测量时长: 60.0 秒
==================================================

正在测量系统基线（算法启动前）...
测量时长: 30.0 秒
[进度显示...]
```

### 步骤 4：启动算法

当看到提示信息时，在**另一个终端**启动算法：

```
==================================================
请在 5 秒内启动算法节点！
节点名称: dynfilter
==================================================
```

例如启动 M-detector：
```bash
roslaunch m_detector m_detector.launch
```

### 步骤 5：等待运行时测量

评估器检测到节点启动后，会自动进行运行时测量：

```
检测到节点已启动: dynfilter

正在测量算法运行时资源占用...
测量时长: 60.0 秒
[进度显示...]
```

### 步骤 6：查看结果

评估完成后，结果会自动保存：

```
==================================================
评估完成！
==================================================
结果已保存到: 
  /path/to/evaluation/results/performance_M_detector_20251219_153045/
  
报告文件:
  - performance_report_20251219_153045.txt
  - performance_data_20251219_153045.json
==================================================
```

## 配置参数详解

### 算法与节点对应关系

| 算法名称 | algorithm_name | node_name |
|---------|----------------|-----------|
| M-detector | "M-detector" | "dynfilter" |
| LV-DOT | "LVDOT" | "detector_node" |
| LDOT | "LDOT" | "ldot_detector_node" |
| FAPP | "FAPP" | "mapping_node" |

### 监控频率选择

| 频率 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| 1 Hz | 系统开销小 | 采样点少 | 长时间稳定测量 |
| 2-3 Hz | 平衡性能和精度 | 适中 | 一般评估 |
| 5 Hz | 采样点多，精度高 | 系统开销大 | 短时间精确测量 |

**建议**：使用 1-2 Hz，既能保证测量精度，又不会对系统造成明显影响。

### 测量时长选择

#### 基线测量时长 (baseline_duration)

| 时长 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| 10-20 秒 | 快速完成 | 基线可能不稳定 | 快速测试 |
| 30 秒 | 平衡速度和准确性 | - | **推荐配置** |
| 60 秒以上 | 基线非常稳定 | 等待时间长 | 精确测量 |

#### 运行时测量时长 (running_duration)

| 时长 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| 30-40 秒 | 快速完成 | 可能未达到稳定状态 | 快速测试 |
| 60 秒 | 算法稳定运行 | - | **推荐配置** |
| 120 秒以上 | 充分评估稳定性 | 测试时间长 | 长期稳定性测试 |

## 报告内容说明

### 文本报告示例

```
==================================================
M-detector 性能评估报告
==================================================

评估时间: 2025-12-19 15:38:14
算法名称: M-detector
节点名称: dynfilter

基线测量时长: 30.0 秒
运行测量时长: 60.0 秒
监控频率: 1.0 Hz

==================================================
测量结果
==================================================

系统基线（算法启动前）:
  CPU 使用率:  21.15 ± 3.74 %
  内存使用:    7743.09 ± 1.01 MB

算法运行时:
  CPU 使用率:  45.84 ± 5.11 %
  内存使用:    8874.15 ± 12.28 MB

==================================================
算法消耗（增量）
==================================================

  CPU 增量:    +24.69 % (+116.8% 相对基线)
  内存增量:    +1131.06 MB (+14.6% 相对基线)

==================================================
说明:
- 基线: 算法启动前的系统资源占用
- 运行时: 算法运行时的系统资源占用
- 增量: 算法实际消耗 = 运行时 - 基线
==================================================
```

### 报告解读

#### 1. 基线数据
```
系统基线（算法启动前）:
  CPU 使用率:  21.15 ± 3.74 %
  内存使用:    7743.09 ± 1.01 MB
```

- **平均值**：测量期间的平均资源占用
- **标准差 (±)**：数据波动程度，越小越稳定
- **含义**：系统本身和其他程序的资源占用

#### 2. 运行时数据
```
算法运行时:
  CPU 使用率:  45.84 ± 5.11 %
  内存使用:    8874.15 ± 12.28 MB
```

- **平均值**：算法运行时的总资源占用
- **标准差**：算法运行的稳定性指标
- **含义**：系统 + 算法的总资源占用

#### 3. 算法消耗（核心指标）
```
算法消耗（增量）:
  CPU 增量:    +24.69 % (+116.8% 相对基线)
  内存增量:    +1131.06 MB (+14.6% 相对基线)
```

- **绝对增量**：算法实际消耗的资源量
- **相对增量**：相对于基线的百分比增长
- **含义**：算法本身的资源开销

### JSON 数据文件

包含完整的原始数据，可用于：
- 绘制资源占用曲线
- 进行统计分析
- 与其他测试结果对比

```json
{
  "algorithm_name": "M-detector",
  "node_name": "dynfilter",
  "timestamp": "2025-12-19 15:38:14",
  "baseline": {
    "cpu_mean": 21.15,
    "cpu_std": 3.74,
    "memory_mean": 7743.09,
    "memory_std": 1.01,
    "samples": [...]
  },
  "running": {
    "cpu_mean": 45.84,
    "cpu_std": 5.11,
    "memory_mean": 8874.15,
    "memory_std": 12.28,
    "samples": [...]
  },
  "increment": {
    "cpu_absolute": 24.69,
    "cpu_relative": 116.8,
    "memory_absolute": 1131.06,
    "memory_relative": 14.6
  }
}
```

## 常见问题

### Q1: 为什么需要先测量基线？

**原因：**
- 系统本身会占用 CPU 和内存
- ROS 核心节点、Gazebo 等也会占用资源
- 基线测量可以排除这些干扰因素

**示例：**
```
系统总占用: 45% CPU
基线占用:   21% CPU
算法消耗:   24% CPU  ← 这才是算法的真实开销
```

### Q2: 标准差代表什么？

**含义：**
- 标准差越小，资源占用越稳定
- 标准差越大，资源占用波动越大

**判断标准：**
- CPU 标准差 < 5%：非常稳定
- CPU 标准差 5-10%：正常波动
- CPU 标准差 > 10%：波动较大，可能存在性能问题

- 内存标准差 < 50 MB：非常稳定
- 内存标准差 50-200 MB：正常波动
- 内存标准差 > 200 MB：波动较大，可能存在内存泄漏

### Q3: 如何判断算法性能是否良好？

**CPU 消耗参考：**
- < 20%：轻量级算法，性能优秀
- 20-40%：中等消耗，性能良好
- 40-60%：较高消耗，需要优化
- > 60%：高消耗，严重影响系统

**内存消耗参考：**
- < 500 MB：轻量级
- 500-1500 MB：中等
- 1500-3000 MB：较高
- > 3000 MB：高消耗

### Q4: 评估器检测不到节点怎么办？

**检查步骤：**

1. **确认节点名称是否正确**
   ```bash
   rosnode list
   ```
   查看实际的节点名称，确保与配置文件中的 `node_name` 一致

2. **检查节点是否真的启动**
   ```bash
   rosnode info /node_name
   ```

3. **修改配置文件**
   如果节点名称不匹配，修改 `performance_config.yaml` 中的 `node_name`

### Q5: 测量结果不稳定怎么办？

**可能原因和解决方法：**

1. **测量时长太短**
   - 增加 `baseline_duration` 和 `running_duration`
   - 建议至少 30 秒和 60 秒

2. **系统负载波动大**
   - 关闭不必要的程序
   - 避免在测量期间进行其他操作

3. **算法本身不稳定**
   - 检查算法是否有性能问题
   - 查看算法日志是否有异常

### Q6: 可以同时评估多个算法吗？

**不可以。** 此功能设计用于单算法评估。

**原因：**
- 多个算法同时运行会相互影响
- 无法准确分离各算法的资源消耗

**建议：**
- 逐个评估每个算法
- 每次评估前重启系统，确保环境一致

### Q7: 如何对比不同算法的性能？

**方法 1：查看报告文件**
- 分别评估每个算法
- 对比各自的"算法消耗（增量）"部分

**方法 2：使用脚本批量处理**
- 编写脚本读取所有 JSON 文件
- 生成对比表格或图表

**示例对比：**
```
算法性能对比（CPU 消耗）:
  LDOT:        15.2%
  LV-DOT:      18.7%
  M-detector:  24.6%
  FAPP:        32.1%
```

## 技术细节

### 资源监控实现

使用 Python `psutil` 库：

```python
# CPU 使用率
cpu_percent = psutil.cpu_percent(interval=1)

# 内存使用量
memory_info = psutil.virtual_memory()
memory_mb = memory_info.used / (1024 * 1024)
```

### 节点检测实现

使用 ROS API：

```python
# 获取所有运行中的节点
nodes = rosnode.get_node_names()

# 检查目标节点是否在列表中
if node_name in nodes:
    # 节点已启动
```

### 统计计算

```python
# 平均值
mean = sum(samples) / len(samples)

# 标准差
variance = sum((x - mean) ** 2 for x in samples) / len(samples)
std = sqrt(variance)
```

## 最佳实践

### 1. 评估前准备

- **关闭不必要的程序**：浏览器、IDE、视频播放器等
- **确保系统稳定**：等待系统启动完成，负载稳定
- **使用相同环境**：对比测试时保持系统环境一致

### 2. 参数配置建议

**快速测试（开发阶段）：**
```yaml
baseline_duration: 10.0
running_duration: 30.0
monitor_freq: 1.0
```

**标准评估（性能测试）：**
```yaml
baseline_duration: 30.0
running_duration: 60.0
monitor_freq: 1.0
```

**精确评估（论文数据）：**
```yaml
baseline_duration: 60.0
running_duration: 120.0
monitor_freq: 2.0
```

### 3. 多次测量

- 每个算法至少测量 3 次
- 取平均值作为最终结果
- 记录标准差评估稳定性

### 4. 结果记录

建议创建表格记录所有测试结果：

| 算法 | CPU (%) | 内存 (MB) | CPU 标准差 | 内存标准差 | 测试日期 |
|-----|---------|----------|-----------|-----------|---------|
| LDOT | 15.2 | 856 | 2.3 | 45 | 2025-12-19 |
| LV-DOT | 18.7 | 1024 | 3.1 | 67 | 2025-12-19 |
| M-detector | 24.6 | 1131 | 5.1 | 89 | 2025-12-19 |
| FAPP | 32.1 | 1456 | 4.2 | 102 | 2025-12-19 |

## 故障排除

### 问题 1：评估器启动失败

**错误信息：**
```
[ERROR] Failed to load parameters
```

**解决方法：**
- 检查 `performance_config.yaml` 文件是否存在
- 检查 YAML 语法是否正确
- 确保文件路径正确

### 问题 2：无法创建输出目录

**错误信息：**
```
[ERROR] Cannot create output directory
```

**解决方法：**
- 检查磁盘空间是否充足
- 检查目录权限
- 手动创建 `results` 目录

### 问题 3：测量数据异常

**现象：**
- CPU 使用率超过 100%
- 内存使用量为负数
- 标准差异常大

**解决方法：**
- 重启系统
- 检查 `psutil` 库是否正确安装
- 降低监控频率

## 相关文件

- **启动文件**：`evaluation/launch/performance_evaluation.launch`
- **配置文件**：`evaluation/config/performance_config.yaml`
- **评估脚本**：`evaluation/scripts/performance_evaluator.py`
- **结果目录**：`evaluation/results/performance_*/`

## 扩展功能建议

### 1. 实时监控界面
- 使用 rqt 插件显示实时资源占用曲线
- 可视化 CPU 和内存变化趋势

### 2. 批量评估
- 自动依次评估多个算法
- 生成统一的对比报告

### 3. 更多监控指标
- GPU 使用率（如果使用 GPU）
- 磁盘 I/O
- 网络流量

### 4. 性能预警
- 设置资源占用阈值
- 超过阈值时自动报警

### 5. 历史数据对比
- 保存历史测试结果
- 自动对比不同版本的性能变化
